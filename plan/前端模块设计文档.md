# 前端模块设计文档

> 版本：v2.0（重构）  ｜  日期：2026-07-21
> 配套：[产品与技术方案.md](file:///c:\Users\young\Desktop\WorkSPace\Notely\workspace\ChatAgent\产品与技术方案.md) ｜ [DESIGN.md](file:///c:\Users\young\Desktop\WorkSPace\Notely\workspace\ChatAgent\DESIGN.md)
> 变更：v1.0 的 Vite SPA 架构重构为 Next.js 14 App Router，支持 SSG/SSR；原两个模块改为频道化融入博客；Markdown 渲染、编辑器、搜索选型调整。

---

## 一、概述

### 1.1 文档目的
定义"个人博客 + AI 知识库问答"前端的整体架构、目录结构、路由、状态管理、核心组件、通信层与构建部署。

### 1.2 前端职责范围
- 公开博客站点：文章浏览、频道、关于、搜索
- AI 问答（贯穿全站）：文字流式问答 + 豆包式语音通话，范围三级（全站/频道/文章）
- 博主后台：文章管理、知识库、频道、人设、数据看板、政策采集

### 1.3 设计原则
1. **SSG/CSR 分层**：博客内容页静态化（SEO），问答交互页客户端渲染（流式）。
2. **内容即知识库**：文章发布自动入库，前端无需维护独立知识库上传流（保留补充上传）。
3. **流式优先**：文字与语音均为流式交互，组件层统一抽象。
4. **频道化**：原 HR 助手、政策查询统一为频道，前端复用同一套频道模板。
5. **本地/云兼容**：配置外置，构建产物不绑定部署环境。

---

## 二、技术栈

| 层 | 选型 | 版本 | 说明 |
|----|------|------|------|
| 框架 | **Next.js** | 14.x (App Router) | SSG/SSR/CSR 混合渲染 |
| 语言 | TypeScript | 5.x | 全量类型 |
| React | React | 18.x | Next.js 内置 |
| 状态管理 | Zustand | 4.x | 客户端交互态（问答/语音） |
| 服务端数据 | **RSC + TanStack Query** | 5.x | 服务端组件直取数；客户端交互用 Query |
| UI 组件 | Ant Design | 5.x | 后台沿用；博客前台配 CSS Modules 定制 |
| 样式 | **CSS Modules + Tailwind** | 3.x | 博客前台用 CSS Modules 隔离风格，后台用 Tailwind |
| 请求 | Axios | 1.x | 客户端请求（REST） |
| 实时通信 | 原生 WebSocket | - | 文字流式 + 语音流式 |
| 博客 Markdown | **MDX + next-mdx-remote** | - | SSG 预渲染，支持嵌入 React 组件 |
| 代码高亮（博客） | **Shiki** | - | VS Code 同款，SSR 友好，质感优先 |
| 代码高亮（对话） | rehype-highlight | - | 轻量，流式场景 |
| 对话 Markdown | react-markdown + remark-gfm | - | CSR 流式增量渲染 |
| 编辑器 | **Bytemd** | - | 源码+预览分屏，插件生态好 |
| 图片 | next/image + react-medium-image-zoom | - | 懒加载 + 点击放大 |
| Mermaid | rehype-mermaid | - | 技术图表 |
| 数学公式 | remark-math + rehype-katex | - | 技术写作 |
| 全文搜索 | **Meilisearch** | - | 中文分词、即时搜索 |
| 图标 | @ant-design/icons + lucide-react | - | 线性风格 |
| 工具库 | dayjs / lodash-es / clsx | - | 常用工具 |
| 代码规范 | ESLint + Prettier + Husky | - | 提交前校验 |
| 测试 | Vitest + React Testing Library | - | 单元/组件测试 |
| SEO | next-sitemap + Metadata API | - | sitemap、og、结构化数据 |

---

## 三、目录结构（Next.js App Router）

```
chatagent-web/
├── app/                          # App Router 路由与页面
│   ├── (public)/                 # 公开站点路由组（博客前台）
│   │   ├── layout.tsx            # 公开站点布局（导航+页脚）
│   │   ├── page.tsx              # 首页（文章流+全站问答入口）
│   │   ├── posts/
│   │   │   ├── page.tsx          # 文章列表（SSG）
│   │   │   └── [slug]/
│   │   │       └── page.tsx      # 文章详情（SSG，含末尾问AI）
│   │   ├── channel/
│   │   │   └── [slug]/
│   │   │       └── page.tsx      # 频道页（SSG/ISR）
│   │   ├── ask/
│   │   │   ├── page.tsx          # 全站问答页（CSR）
│   │   │   └── voice/
│   │   │       └── page.tsx      # 语音通话页（CSR）
│   │   ├── about/
│   │   │   └── page.tsx          # 关于（SSG）
│   │   └── search/
│   │       └── page.tsx          # 搜索（CSR，Meilisearch）
│   ├── admin/                    # 博主后台（CSR，鉴权）
│   │   ├── layout.tsx            # 后台布局
│   │   ├── page.tsx              # 数据看板
│   │   ├── posts/
│   │   │   ├── page.tsx          # 文章管理
│   │   │   ├── new/
│   │   │   │   └── page.tsx      # 写文章（Bytemd）
│   │   │   └── [id]/edit/
│   │   │       └── page.tsx      # 编辑
│   │   ├── knowledge/
│   │   │   └── page.tsx          # 知识库管理
│   │   ├── channels/
│   │   │   ├── page.tsx          # 频道管理
│   │   │   └── [id]/
│   │   │       └── page.tsx      # 频道配置
│   │   ├── persona/
│   │   │   └── page.tsx          # 人设配置
│   │   ├── policy-collector/
│   │   │   └── page.tsx          # 政策采集管理
│   │   └── settings/
│   │       └── page.tsx          # 站点设置
│   ├── login/
│   │   └── page.tsx              # 登录
│   ├── layout.tsx                # 根布局
│   ├── globals.css               # 全局样式
│   ├── not-found.tsx             # 404
│   └── error.tsx                 # 错误边界
├── components/                   # 通用组件
│   ├── chat/                     # 对话通用组件
│   │   ├── MessageList.tsx
│   │   ├── MessageItem.tsx
│   │   ├── StreamingText.tsx     # 流式打字机（react-markdown）
│   │   ├── Citation.tsx          # 引用溯源
│   │   ├── FollowUpQuestions.tsx # 推荐追问
│   │   └── ClarifyCard.tsx       # 主动追问
│   ├── voice/                    # 语音通话通用组件
│   │   ├── VoiceCallPanel.tsx
│   │   ├── CallStatus.tsx
│   │   └── WaveVisualizer.tsx
│   ├── blog/                     # 博客通用组件
│   │   ├── ArticleContent.tsx    # MDX 渲染容器
│   │   ├── TableOfContents.tsx   # 目录
│   │   ├── ArticleCard.tsx       # 文章卡片
│   │   ├── TagList.tsx
│   │   └── ReadingTime.tsx
│   ├── editor/                   # 编辑器
│   │   └── MarkdownEditor.tsx    # Bytemd 封装
│   ├── common/                   # 通用 UI
│   └── layout/                   # 布局组件
├── lib/                          # 业务逻辑层
│   ├── api/                      # REST 请求
│   │   ├── request.ts            # Axios 实例
│   │   ├── posts.ts              # 文章
│   │   ├── channels.ts           # 频道
│   │   ├── knowledge.ts          # 知识库
│   │   ├── persona.ts            # 人设
│   │   └── policy.ts             # 政策
│   ├── services/                 # WebSocket/流式
│   │   ├── chatStream.ts         # 文字流式
│   │   └── voiceCall.ts          # 语音流式管道
│   ├── search/                   # Meilisearch 客户端
│   │   └── client.ts
│   ├── stores/                   # Zustand
│   │   ├── chatStore.ts
│   │   └── voiceStore.ts
│   └── mdx/                      # MDX 渲染配置
│       ├── serialize.ts          # next-mdx-remote 序列化（Shiki+插件）
│       └── components.tsx        # MDX 自定义组件映射
├── hooks/                        # 通用 Hooks
│   ├── useChat.ts
│   ├── useVoiceCall.ts
│   ├── useStreamRender.ts
│   └── usePermission.ts
├── types/                        # 全局类型
├── utils/                        # 工具函数
│   ├── audio.ts                  # 音频处理
│   ├── format.ts
│   └── download.ts
├── public/                       # 静态资源
├── .env.development
├── .env.production
├── next.config.js
├── tailwind.config.js
├── tsconfig.json
└── package.json
```

**分层约定**
- `app/` 只做路由与页面组装，逻辑下沉到 `lib/` 与 `components/`。
- `lib/api/` REST 请求，`lib/services/` WebSocket 流式，均与 React 解耦。
- `hooks/` 是组件与 services/stores 的桥梁，组件只调 hooks。
- 服务端组件（RSC）直接 `await fetch`，客户端组件用 `'use client'` 声明。

---

## 四、路由与渲染策略

### 4.1 路由表与渲染方式

| 路由 | 渲染 | 鉴权 | 说明 |
|------|------|------|------|
| `/` | SSG/ISR | 公开 | 首页，文章流 + 全站问答入口 |
| `/posts` | SSG/ISR | 公开 | 文章列表 |
| `/posts/[slug]` | **SSG** | 公开 | 文章详情，`generateStaticParams` 预生成 |
| `/channel/[slug]` | SSG/ISR | 公开 | 频道页 |
| `/ask` | **CSR** | 公开 | 问答页（流式交互） |
| `/ask/voice` | **CSR** | 公开 | 语音通话页 |
| `/search` | CSR | 公开 | Meilisearch 搜索 |
| `/about` | SSG | 公开 | 关于 |
| `/admin/*` | CSR | 博主 | 后台，登录鉴权 |
| `/login` | CSR | 公开 | 登录 |

### 4.2 SSG 实现要点
- 文章详情页用 `generateStaticParams` 预生成所有已发布文章的静态页。
- 文章更新时通过 **按需重新生成**（On-Demand ISR）：后端发布文章后调 Next.js 的 revalidate API 触发重新生成。
- 首页/列表/频道用 ISR，设定刷新间隔或按需触发。

### 4.3 SEO 要点
- 每页用 Next.js Metadata API 配 `title`/`description`/`openGraph`。
- `next-sitemap` 自动生成 sitemap.xml 与 robots.txt。
- 文章页加 JSON-LD 结构化数据（Article schema）。
- MDX 内容 SSG 预渲染为 HTML，搜索引擎可直接抓取。

### 4.4 鉴权
- 公开站点无鉴权。
- `/admin/*` 用中间件（middleware.ts）拦截，未登录跳 `/login?redirect=...`。
- 博主 token 存 httpOnly cookie，中间件校验。

---

## 五、状态管理

### 5.1 状态划分原则
- **服务端状态**（文章列表、频道、配置）→ RSC 直取或 TanStack Query。
- **客户端交互状态**（当前会话、输入草稿、通话状态）→ Zustand。
- **局部 UI 状态** → 组件内 useState。

### 5.2 Zustand Store 设计

#### chatStore（客户端组件）
```ts
interface ChatState {
  scope: { type: 'global' | 'channel' | 'article'; refId?: string };
  sessionId: string | null;
  messages: ChatMessage[];
  streaming: boolean;
  followUps: string[];
  setScope: (s: ChatState['scope']) => void;
  appendMessage: (m: ChatMessage) => void;
  appendToken: (id: string, token: string) => void;
}
```

#### voiceStore（客户端组件）
```ts
interface VoiceState {
  status: 'idle' | 'connecting' | 'connected' | 'speaking' | 'listening' | 'ended' | 'error';
  micMuted: boolean;
  transcript: string;
  aiText: string;
  interruptible: boolean;
  setStatus: (s: VoiceState['status']) => void;
  appendTranscript: (t: string) => void;
  appendAiText: (t: string) => void;
  reset: () => void;
}
```

> Zustand 仅在客户端组件中使用，服务端组件不依赖。

---

## 六、核心组件设计

### 6.1 博客文章渲染（SSG）

#### ArticleContent（MDX 容器）
```
ArticleContent
├── TableOfContents          # 目录（锚点跳转，滚动高亮）
├── MDXRemote                # next-mdx-remote 渲染
│   └── 自定义组件映射：<Code>, <Callout>, <Image>, <Mermaid>
├── ReadingTime              # 阅读时长
└── ArticleAskEntry          # 文末"问 AI"入口
```

- 文章 Markdown 在服务端通过 `serialize`（next-mdx-remote）预渲染为 HTML，Shiki 高亮代码。
- 客户端只做目录滚动监听、图片缩放等交互。
- MDX 支持自定义组件（如 `<Callout>` 提示框），提升博客表达力。

#### 渲染管线
```
Markdown 源码 → remark/rehype 插件链 → Shiki 高亮 → MDX 序列化 → SSG HTML
插件：remark-gfm, remark-math, rehype-katex, rehype-slug,
      rehype-autolink-headings, rehype-mermaid, remark-reading-time
```

### 6.2 对话组件体系（文字问答，CSR）

```
ChatContainer                    # 'use client'
├── MessageList
│   └── MessageItem
│       ├── StreamingText        # react-markdown 流式渲染
│       ├── Citation             # 引用溯源（指向文章链接）
│       └── FollowUpQuestions    # 推荐追问
├── InputBox
└── VoiceEntryButton             # 切语音
```

#### StreamingText（流式渲染）
- 接收 token 流，逐字追加渲染 `react-markdown`。
- 用 `requestAnimationFrame` 节流，按段落缓冲避免表格/代码块闪烁。
- 末尾光标 `▋` 闪烁表示生成中。
- 代码高亮用 rehype-highlight（轻量，适合流式）。

#### Citation（引用溯源）
- 每条 AI 消息携带 `citations: { articleId, title, slug, snippet }[]`。
- 点击展开原文片段，"查看原文"跳转 `/posts/:slug`。

### 6.3 提示询问·主动追问

```ts
type ChatMessage =
  | { role: 'user'; content: string }
  | { role: 'assistant'; content: string; citations?: Citation[]; followUps?: string[] }
  | { role: 'assistant'; type: 'clarify'; content: string; options?: string[] };
```

- `clarify` 消息渲染为 `ClarifyCard` 带选项卡片，点选项自动回复。

### 6.4 语音通话组件（CSR，全站通用）

```
VoiceCallPanel                   # 'use client'
├── CallStatus                   # 状态显示（聆听/思考/播报）
├── WaveVisualizer               # 双向波形（Canvas）
├── LiveTranscript               # 双向实时字幕
├── ControlBar                   # 静音/挂断/切文字
└── FallbackTextChat             # 降级文字
```

详见第八节《语音通话前端实现》。

### 6.5 编辑器（后台）

#### MarkdownEditor（Bytemd 封装）
```
MarkdownEditor                   # 'use client'
├── Bytemd 核心编辑器             # 源码+预览分屏
├── 工具栏：图片上传 / 表格 / 代码块 / 预览切换
├── 图片上传                     # 粘贴/拖拽 → 对象存储 → 返回 URL
└── 草稿自动保存                  # 防抖 30s
```

- Bytemd 插件：gfm、math、mermaid、highlight（与展示端一致）。
- 编辑器产出 Markdown 源码，后端存储，展示端 MDX 渲染，两端插件链一致保证所见即所得。

### 6.6 频道组件

```
ChannelPage                      # 频道页（SSG/ISR）
├── ChannelHeader                # 频道名/简介/人设提示
├── ChannelAskEntry              # 频道问答入口（scope=channel）
└── ArticleList                  # 频道文章列表
```

- 频道问答复用 `ChatContainer`，仅 `scope` 不同。
- 政策频道额外提供多维检索入口（时效/主题/单位筛选）。

---

## 七、通信层设计

### 7.1 REST（Axios，客户端组件）

`lib/api/request.ts` 统一封装：
- `baseURL` 来自环境变量。
- 请求拦截器注入 `Authorization`（后台）或匿名会话 ID（公开问答）。
- 响应拦截器统一错误处理。

主要 REST 接口：

| 模块 | 接口 | 说明 |
|------|------|------|
| 文章 | `GET /api/posts` | 列表 |
| 文章 | `GET /api/posts/:slug` | 详情（RSC 直取） |
| 频道 | `GET /api/channels` | 频道列表 |
| 频道 | `GET /api/channels/:slug` | 频道详情 |
| 知识库 | `POST /api/knowledge/upload` | 补充上传 |
| 知识库 | `GET /api/knowledge/docs` | 文档列表 |
| 对话 | `GET /api/chat/sessions` | 会话列表 |
| 政策 | `GET /api/policy/search` | 政策检索 |
| 后台 | `POST /api/admin/posts` | 写文章 |
| 后台 | `POST /api/admin/channels` | 建频道 |

### 7.2 RSC 直取数据

服务端组件直接 `await fetch()`，配合 Next.js fetch 缓存：
```tsx
// app/(public)/posts/[slug]/page.tsx
export default async function Page({ params }) {
  const post = await fetch(`${API}/posts/${params.slug}`, { next: { revalidate: 60 } }).then(r => r.json());
  const mdxSource = await serialize(post.content, { mdxOptions });
  return <ArticleContent source={mdxSource} />;
}
```

### 7.3 WebSocket（流式，客户端组件）

#### 文字流式问答
- 连接：`wss://host/ws/chat?scope=global|channel:xxx|article:xxx`
- 协议：JSON 帧。

```ts
// C→S
{ type: 'user_message', content: 'xxx' }
{ type: 'stop' }

// S→C
{ type: 'token', content: 'xxx' }
{ type: 'citation', data: {...} }
{ type: 'followup', questions: [...] }
{ type: 'clarify', content: '...', options: [...] }
{ type: 'done' }
{ type: 'error', message: '...' }
```

#### 语音流式通话
- 连接：`wss://host/ws/voice`
- 协议：二进制音频帧 + JSON 控制帧混传（首字节区分）。
- 详见第八节。

### 7.4 services 层封装

`lib/services/chatStream.ts`：
```ts
export class ChatStreamClient {
  constructor(private ws: WebSocket) {}
  onToken(cb: (t: string) => void): void;
  onCitation(cb: (c: Citation) => void): void;
  onFollowUp(cb: (q: string[]) => void): void;
  onClarify(cb: (c: Clarify) => void): void;
  onDone(cb: () => void): void;
  send(content: string): void;
  stop(): void;
  close(): void;
}
```

`hooks/useChat.ts` 把 `ChatStreamClient` 与 `chatStore` 绑定：
```ts
const { send, stop, streaming } = useChat({ type: 'global' });
```

---

## 八、语音通话前端实现（豆包式体验）

### 8.1 交互状态机

```
        ┌─────────┐  点击通话   ┌────────────┐
        │  idle   │ ──────────► │ connecting │
        └─────────┘             └─────┬──────┘
            ▲                         │ WS 建立 + 麦克风授权
            │ 挂断                     ▼
        ┌───┴──────┐  用户开口   ┌──────────┐  AI 开口   ┌──────────┐
        │  ended   │ ◄────────── │ listening │ ─────────► │ speaking │
        └──────────┘             └──────────┘             └────┬─────┘
              ▲                                                │
              └─────────── 用户开口打断 ──────────────────────┘
```

### 8.2 音频采集
```ts
async function getMicStream(): Promise<MediaStream> {
  return navigator.mediaDevices.getUserMedia({
    audio: {
      channelCount: 1,
      sampleRate: 16000,
      echoCancellation: true,   // 回声消除（关键，否则 AI 声音被回采）
      noiseSuppression: true,
      autoGainControl: true,
    },
  });
}
```
- 用 `AudioWorklet` 采集 PCM，16kHz 单声道，每帧 ~20ms。
- **回声消除必开**：否则 TTS 声音被回采触发误打断。

### 8.3 前端 VAD
- `silero-vad`（WASM）判断用户说话起止。
- 用户开口 → 触发 `interrupt` → 后端停 TTS。
- 用户停顿超 600ms → 触发 `utterance_end` → 后端生成回复。

### 8.4 WebSocket 协议（二进制 + 控制帧）

| 首字节 | 类型 | 方向 | 内容 |
|--------|------|------|------|
| `0x01` | 音频帧 PCM | C→S | 16kHz PCM |
| `0x02` | ASR 文本增量 | S→C | `{ text }` |
| `0x03` | LLM token | S→C | `{ token }` |
| `0x04` | TTS 音频帧 | S→C | AI 语音 PCM/Opus |
| `0x10` | 控制帧 JSON | 双向 | 见下 |

控制帧：
```ts
// C→S
{ type: 'start_call' }
{ type: 'interrupt' }
{ type: 'mute', muted: boolean }
{ type: 'end_call' }

// S→C
{ type: 'asr_final', text }
{ type: 'ai_start' }
{ type: 'ai_end' }
{ type: 'status', status: 'listening'|'speaking' }
{ type: 'error', message }
```

### 8.5 TTS 播放队列
- 收到 `0x04` → 入 `AudioWorklet` 播放队列，边收边播，首音到达即播。
- 收到 `interrupt` → 清空队列停止播放。

### 8.6 useVoiceCall Hook
```ts
function useVoiceCall() {
  return {
    status, start, hangup, toggleMute, interrupt,
    micLevel, aiLevel,        // 波形电平
    transcript, aiText,       // 双向字幕
    fallbackToText,
  };
}
```

### 8.7 降级与兜底
- 麦克风授权失败 → 提示回退文字。
- WS 断连 → 重连 1 次，失败回退文字。
- 不支持 AudioWorklet → 降级 ScriptProcessor。

### 8.8 性能要点
- 音频处理在 Worklet 线程，主线程只做 UI。
- 波形用 Canvas + RAF，不触发 React 重渲染。
- 转写文本用 ref + DOM 直更，避免每 token 重渲染。

---

## 九、全文搜索（Meilisearch）

### 9.1 前端集成
```ts
// lib/search/client.ts
import { MeiliSearch } from 'meilisearch';
export const searchClient = new MeiliSearch({ host: MEILI_HOST, apiKey: MEILI_KEY });
```

### 9.2 搜索页
- 输入框防抖 300ms，调 Meilisearch 即时返回。
- 结果高亮命中词，点击跳转文章或发起问答。
- 支持按频道/标签筛选。

### 9.3 索引同步
- 后端文章发布时同步写入 Meilisearch 索引（Celery 任务）。

---

## 十、权限与中间件

### 10.1 角色与访问
| 角色 | 访问范围 |
|------|---------|
| 访客 | 公开站点全部 + 公开问答 |
| 博主 | 全部 + `/admin/*` |

### 10.2 中间件实现
```ts
// middleware.ts
export function middleware(req: NextRequest) {
  const { pathname } = req.nextUrl;
  if (pathname.startsWith('/admin')) {
    const token = req.cookies.get('admin_token');
    if (!token) return NextResponse.redirect(new URL('/login?redirect=' + pathname, req.url));
  }
}
export const config = { matcher: ['/admin/:path*'] };
```

---

## 十一、构建与部署

### 11.1 环境变量
```
# .env.development
NEXT_PUBLIC_API_BASE=http://localhost:8000
NEXT_PUBLIC_WS_BASE=ws://localhost:8000
NEXT_PUBLIC_MEILI_HOST=http://localhost:7700
MEILI_KEY=xxx

# .env.production
NEXT_PUBLIC_API_BASE=/api
NEXT_PUBLIC_WS_BASE=/ws
NEXT_PUBLIC_MEILI_HOST=/meili
```

### 11.2 构建
- `next build` 产出 `.next/`，自动 SSG 静态页 + CSR 按需。
- 本地：`next start` 或 PM2 托管。
- 上云：CDN 托管静态资源 + Node Server 跑 SSR/ISR + 对象存储。

### 11.3 Nginx 示例
```nginx
location / { proxy_pass http://nextjs:3000; }    # Next.js server
location /api/ { proxy_pass http://backend:8000; }
location /ws/ {
  proxy_pass http://backend:8000;
  proxy_http_version 1.1;
  proxy_set_header Upgrade $http_upgrade;
  proxy_set_header Connection "upgrade";
  proxy_read_timeout 3600s;
}
```

---

## 十二、开发规范

1. **服务端/客户端组件**：默认 RSC，需要交互才加 `'use client'`，避免不必要的水合。
2. **命名**：组件 PascalCase，hooks 以 `use` 开头。
3. **类型**：禁止 `any`，API 响应有 interface。
4. **提交**：Conventional Commits，Husky + lint-staged 校验。
5. **样式**：博客前台 CSS Modules 隔离风格，后台 Tailwind。
6. **流式渲染**：统一走 `useStreamRender`，禁止组件直接操作 WS。
7. **音频**：逻辑封装在 `utils/audio.ts` 与 `lib/services/voiceCall.ts`，组件不直接接触 `AudioContext`。
8. **MDX**：博客文章插件链与 Bytemd 编辑器插件链保持一致，保证所见即所得。

---

## 十三、与 v1.0 的主要差异

| 维度 | v1.0 | v2.0 |
|------|------|------|
| 框架 | Vite SPA | Next.js 14 App Router |
| 渲染 | 纯 CSR | SSG 博客页 + CSR 问答页 |
| 路由 | React Router | Next.js 文件路由 |
| 模块结构 | 两个独立 modules | 频道化，复用频道模板 |
| Markdown 展示 | react-markdown + highlight.js | MDX + Shiki（博客）/ react-markdown + highlight.js（对话） |
| 编辑器 | Tiptap (WYSIWYG) | Bytemd (源码+预览) |
| 搜索 | PG 全文索引 | Meilisearch |
| 鉴权 | 双守卫 | Next.js middleware |
| 问答范围 | 固定模块 | 三级 scope（全站/频道/文章） |
